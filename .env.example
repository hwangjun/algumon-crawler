# 🛒 알구몬 크롤링 서버 환경변수

# Supabase 설정 (hotdeal-nextjs와 동일한 DB 사용)
SUPABASE_URL=https://your-project.supabase.co

# 🔑 SERVICE_ROLE_KEY 필수! (쓰기 권한 필요)
# Supabase Dashboard → Settings → API → service_role (secret)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Fallback: anon key (읽기 전용, RLS로 쓰기 차단됨)
SUPABASE_ANON_KEY=your-anon-key

# 또는 Next.js 호환 변수명
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# 서버 설정
PORT=3000
NODE_ENV=production

# 크롤링 설정
CRAWL_INTERVAL=300  # 크롤링 간격 (초) - 5분
MAX_ITEMS_PER_CATEGORY=20  # 카테고리당 최대 아이템 수
REQUEST_TIMEOUT=30000  # HTTP 요청 타임아웃 (밀리초)

# Render.com 최적화
RENDER_EXTERNAL_URL=https://your-app-name.onrender.com