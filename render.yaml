# 🛒 Render.com Web Service 설정
# 알구몬 크롤링 Web Service v2.1

services:
  - type: web
    name: algumon-crawler
    env: node
    buildCommand: npm install --only=production
    startCommand: npm start
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_ANON_KEY
        sync: false
      - key: PORT
        value: 3000
    scaling:
      minInstances: 1
      maxInstances: 1
    autoDeploy: true
    branch: main
    
# Web Service 특징:
# - Express 서버 (포트 3000)
# - HTTP API 엔드포인트 제공
# - 크롤링 + API 동시 제공
# - 수동 크롤링 트리거 가능
# - 실시간 상태 조회 가능
# - Health check 엔드포인트